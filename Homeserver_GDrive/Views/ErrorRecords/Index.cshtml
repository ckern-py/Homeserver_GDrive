@inject IConfiguration Configuration
@using GDriveWorker.Metadata
@model ErrorRecordsViewModel
@{
    ViewData["Title"] = "Error Records";
}

<div class="text-center">
    <h2 class="display-4">Running service as:</h2>

    <b>@Model.ServiceAccountName</b>
</div>

<body>
    <div class="text-center">
        <br />
        <b>Error Records:</b> Showing @Configuration["AppSettings:DetailErrorRecordsCount"] of @Model.ErrorCount records
        <br />
    </div>
    @foreach (BasicTableInfo uploadInfo in Model.ErrorInfo)
    {
        <div class="container pt-2 w-100">
            <div class="row ">
                <div class="col-sm-4 w-50" style=" border: 1px solid;">
                    <div>
                        <b>FileName:</b> @uploadInfo.TableMessage
                    </div>
                </div>
                <div class="col-sm-4 w-50" style=" border: 1px solid;">
                    <div>
                        <b>Upload Time:</b> @uploadInfo.MessageDT
                    </div>
                </div>
            </div>
        </div>
    }
</body>